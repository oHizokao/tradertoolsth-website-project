const puppeteer = require('puppeteer'); (async () => { const browser = await puppeteer.launch(); const page = await browser.newPage(); page.on('console', msg => console.log('PAGE LOG:', msg.text())); await page.goto('http://127.0.0.1:3000/Version-2-Gold-Trading/admin.html?cb=' + Date.now()); await new Promise(r => setTimeout(r, 2000)); await page.evaluate(() => { console.log('News items:', document.querySelectorAll('.admin-news-table tbody tr').length); }); await new Promise(r => setTimeout(r, 1000)); await browser.close(); })();
